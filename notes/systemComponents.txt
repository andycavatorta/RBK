

packageManager
    automatic git update
    versions and script execution



logging
    status dashboard


networking
    structure:
        many hosts
        one server
            vimina

    broadcast and discovery

    


network discovery and config
logging system
load code based on hostname
top-level multiprocessing manager

clients:
    receive and parse OSC:
        can this be moved from code into a config file?
        or make code as easy to read as a config file
    FPGA:
        signal generator
        parallel communication
    MIDI:

    Motor Controller:

ipblink

